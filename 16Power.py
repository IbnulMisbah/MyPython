# now i will write a programm to Operate Power (**)=(^).

base = float(input('Input your base a : '))
powe = float(input("Input a's power b : "))

power = base**powe

print ("a to the power b is :",power)
